# Example environment variables for local docker-compose
# Copy this to .env and adjust if needed before running docker-compose

# Postgres connection that points to the postgres service inside compose
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/nestjs_db?schema=public

# Redis connection - the hostname 'redis' refers to the redis service in docker-compose
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379

# App settings
NODE_ENV=production
PORT=3000

